/**
 * P_putchar - writes charachter to stdout
 * Return: 1 Onsuccess.
 * On error, -1 is returned.
 */
int _putchar(char);

/**
 * print_alphabet(): prints the alphabet, in lowercase.
 */
void print_alphabet(void);
