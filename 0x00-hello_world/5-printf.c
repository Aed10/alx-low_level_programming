#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{	/* Printf to print a string in the output std. */
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0); /* return 0 if all is ok */
}
